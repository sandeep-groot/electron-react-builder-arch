import React from 'react';
import NavBar from '../../NavBar';

const Header = () => {
  return (
    <header className="border-bottom pb-1">
      <NavBar />
    </header>
  );
};

export default Header;
